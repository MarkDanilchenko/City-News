// const addToFavorites = document.getElementById('addToFavorites');
// if (addToFavorites) {
// 	addToFavorites.addEventListener('click', (e) => {
// 		let article = document.getElementById('articleTitle').textContent;
// 		alert(`${article} is added to favorites!`);
// 	});
// }
